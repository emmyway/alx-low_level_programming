#include "main.h"

/**
 * times_table- prints the 9 times table
 * Return: nothing
 */

void times_table(void)
{
	int a0, a1, a2, a3, a4, a5, a6 a7, a8, a9;

	for (a0 = '0'; a0 >= '0' && a0 <= '9'; a0++)
	{
		
		_putchar(0 * a0);
	}
	for (a1 = '0'; a1 >= '0' && a1 <= '9'; a1++)
	{
		_putchar(1 * a1);
	}

	for (a2 = '0'; a2 >= '0' && a2 <= '9'; a2++)
	{
		_putchar(2 * a2);
	}

	for (a3 = '0'; a3 >= '0' && a3 <= '9'; a3++)
	{
		_putchar(3 * a3);
	}

	for (a4 = '0'; a4 >= '0' && a4 <= '9'; a4++)
	{
		_putchar(4 * a4);
	}

	for (a5 = '0'; a5 >= '0' && a5 <= '9'; a5++)
	{
		_putchar(5 * a5);
	}

	for (a6 = '0'; a6 >= '0' && a6 <= '9'; a6++)
	{
		_putchar(6 * a6);
	}

	for (a7 = '0'; a7 >= '0' && a7 <= '9'; a7++)
	{
		_putchar(7 * a7);
	}

	for (a8 = '0'; a8 >= '0' && a8 <= '9'; a8++)
	{
		_putchar(8 * a8);
	}

	for (a9 = '0'; a9 >= '0' && a9 <= '9'; a9++)
	{
		_putchar(9 * a9);
	}
